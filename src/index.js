import core, { effect$, closeForeground, setSystemUiVisibility } from '@auto.pro/core'

core({
    baseWidth: 1920,
    baseHeight: 1080,
    needCap: true,
    needService: true,
    needForeground: true
});


